{
    "name": "phoenix",
    "version": "0.14.0",
    "scripts": {
        "compile": "babel -d lib/ phoenix/web/static/js",
        "prepublish": "npm run compile"
    },
    "main": "lib/phoenix.js",
    "dependencies": {
        "core-js": "^0.6.0"
    },
    "devDependencies": {
        "babel": "^4.6.0"
    }
}
